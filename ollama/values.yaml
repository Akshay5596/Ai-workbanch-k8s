image:
  repository: ollama/ollama
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 11434

persistence:
  enabled: true
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce
  size: 20Gi

models:
  pullOnInstall: true
  list:
    - tinyllama
    - nomic-embed-text

resources: {}
