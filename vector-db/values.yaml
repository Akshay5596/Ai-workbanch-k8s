image:
  repository: ankane/pgvector
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 5432

postgres:
  user: postgres
  password: postgres
  database: ragdb

persistence:
  enabled: true
  storageClassName: auto-ebs-sc
  accessModes:
    - ReadWriteOnce
  size: 10Gi

resources: {}
