image:
  repository: ankane/pgvector
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 5432

postgres:
  user: postgres
  password: postgres
  database: ragdb

persistence:
  enabled: true
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce
  size: 10Gi

resources: {}
